import { LogIn, ChevronRight, Stethoscope, X } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { login } from '../api'
export default function Login() { const navigate = useNavigate(); const [form, setForm] = useState({ email: '', password: '' }); const [error, setError] = useState(''); const submit = async e => { e.preventDefault(); setError(''); try { const data = await login(form); localStorage.setItem('clinicdesk_token', data.access_token); navigate('/dashboard') } catch (err) { setError(err.message) } }; return <main className="auth-page"><AuthAside/><form className="auth-card" onSubmit={submit}><span className="feature-icon mint"><LogIn/></span><p className="eyebrow">STAFF ACCESS</p><h2>Welcome back</h2><p className="muted">Sign in to your clinic workspace.</p><label>Email<input required type="email" value={form.email} onChange={e => setForm({...form, email: e.target.value})}/></label><label>Password<input required minLength="8" type="password" value={form.password} onChange={e => setForm({...form, password: e.target.value})}/></label>{error && <div className="error"><X size={16}/>{error}</div>}<button className="primary full">Sign in <ChevronRight size={18}/></button><Link className="link-button" to="/register">Need an account? Register</Link></form></main> }
export function AuthAside() { return <div className="auth-aside"><Link className="brand" to="/"><span className="brand-mark"><Stethoscope size={18}/></span> ClinicDesk</Link><div><p className="eyebrow">THE FRONT DESK, REFINED</p><h1>Make room for better conversations.</h1><p>Less time untangling calendars. More time making patients feel looked after.</p></div></div> }
