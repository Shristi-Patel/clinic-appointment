import { ChevronRight, UserPlus, X } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { register } from '../api'
import { AuthAside } from './Login'
export default function Register() { const navigate = useNavigate(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const submit = async e => { e.preventDefault(); setError(''); try { const data = await register(form); localStorage.setItem('clinicdesk_token', data.access_token); navigate('/dashboard') } catch (err) { setError(err.message) } }; return <main className="auth-page"><AuthAside/><form className="auth-card" onSubmit={submit}><span className="feature-icon mint"><UserPlus/></span><p className="eyebrow">STAFF ACCESS</p><h2>Create your account</h2><p className="muted">Set up your clinic desk in a minute.</p><label>Full name<input required value={form.name} onChange={e => setForm({...form, name: e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e => setForm({...form, email: e.target.value})}/></label><label>Password<input required minLength="8" type="password" value={form.password} onChange={e => setForm({...form, password: e.target.value})}/></label>{error && <div className="error"><X size={16}/>{error}</div>}<button className="primary full">Create account <ChevronRight size={18}/></button><Link className="link-button" to="/login">Already have an account? Sign in</Link></form></main> }
